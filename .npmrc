# @vietz-dev/* (e.g. hono-effect) is published to GitHub Packages, not npmjs.
# NODE_AUTH_TOKEN needs `read:packages`; locally `export NODE_AUTH_TOKEN=$(gh auth token)`.
@vietz-dev:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
