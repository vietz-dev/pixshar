# Pixshar — Claude Code Session Prompt

## Knowledge bundle

A structured knowledge base for this project lives at `.knowledge/` in OKF format (one markdown file per concept, YAML frontmatter). Read it to understand architecture, data model, API design, and the rationale behind key decisions — especially before proposing changes that touch multiple subsystems.

Start with `.knowledge/index.md` for an overview of what is available. Cross-links between leaf concept files use bundle-relative paths (`/architecture/storage.md` etc.).

To update the knowledge bundle after significant changes, run `/knowledge-housekeeping`.

## What we're building
Pixshar is a self-hostable, private event photo sharing app. The photographer (admin) creates
password-protected galleries, uploads photos, and shares a link. Guests unlock the gallery with
a password, browse photos, and can upload their own.

## Stack
- **Frontend**: Next.js 15 (App Router, `output: standalone`)
- **Component Library**: Chakra UI v3 (`@chakra-ui/react`, Emotion runtime) — components *and* layout
- **Backend**: Hono + Bun + Effect
- **Auth**: BetterAuth — single admin account (seeded, no public sign-up), per-gallery JWT cookies for guests
- **ORM**: Prisma + Postgres (CNPG/external-first; bundled `pixshar-postgres` fallback in Helm)
- **Storage**: S3-compatible via AWS SDK (Minio bundled for self-hosting)
- **Infra**: Docker Compose + Helm chart

## Monorepo structure
```
pixshar/
├── apps/
│   ├── api/                        # Hono + Bun + Effect
│   │   ├── src/
│   │   │   ├── lib/                # env.ts, prisma.ts, s3.ts, auth.ts
│   │   │   ├── middleware/         # requireAdmin.ts, requireGallerySession.ts
│   │   │   ├── routes/             # events.ts, gallery.ts, upload.ts
│   │   │   ├── services/           # imageProcessor.ts (Effect pipeline)
│   │   │   └── index.ts
│   │   └── prisma/schema.prisma
│   └── web/                        # Next.js 15
│       └── src/app/
│           ├── admin/              # page.tsx (event list) — DONE
│           ├── gallery/[slug]/     # page.tsx (password gate) — DONE
│           └── auth/login/         # page.tsx — DONE
├── packages/contracts/src/         # oRPC contract — shared request/response shapes
├── docker-compose.yml
└── helm/pixshar/
```

## Key architecture decisions (don't revisit these)
- **Presigned URLs**: photos are never proxied through the API; the browser fetches directly
  from S3/Minio using time-limited presigned URLs generated by the API
- **Gallery sessions**: guests get a JWT cookie scoped to a single event after entering the
  password — no user accounts
- **Image processing**: durable in-process queue (the `Photo` row IS the job). `resizeWorker.ts`
  claims PENDING rows with Postgres `FOR UPDATE SKIP LOCKED` (multi-replica safe), resizes to
  1920px display + 400px thumb in parallel → uploads both to S3 → marks PROCESSED. Retry/backoff
  + crash recovery (stale-PROCESSING reaper via claim lease) are owned by the queue, not Effect.
  (Supersedes the earlier "eager Effect.fork, not queued" decision.)
- **One S3 bucket, path-prefixed**: `{eventId}/originals/`, `{eventId}/display/`, `{eventId}/thumbs/`
- **Single admin**: BetterAuth with `disableSignUp: true`, admin created via seed script on
  first container start

## Data model (Prisma)
```
User         — BetterAuth managed (+ Session, Account, Verification tables)
Event        — id, slug, name, description, passwordHash, status (PROCESSING|READY), createdById
Photo        — id, eventId, photographerName, originalKey, displayKey, thumbKey,
               status (PENDING|PROCESSING|PROCESSED|FAILED), uploadedBy (ADMIN|GUEST),
               + durable-queue cols: attempts, lastError, claimedAt, claimedBy, nextAttemptAt
```

## API routes
```
POST /api/auth/*                           BetterAuth (admin login/session)
GET  /api/events                           List events [admin]
POST /api/events                           Create event [admin]
GET  /api/events/:id                       Get event + photos [admin]
DELETE /api/events/:id                     Delete event + S3 cleanup [admin]
POST /api/gallery/:slug/unlock             Password check → set gallery cookie
GET  /api/gallery/:slug                    Gallery + presigned URLs [gallery session]
POST /api/gallery/:slug/upload             Guest upload [gallery session]
POST /api/upload/events/:id/photos         Admin upload [admin]
GET  /api/upload/events/:id/photos/status  Processing status poll [admin]
GET  /health                               Health check
```

## What's done
- Full API implementation (all routes above)
- Effect-based image processing pipeline with retry
- BetterAuth config (admin-only, gallery JWT middleware)
- Prisma schema + seed script
- Docker Compose (api + web + minio + minio-init)
- Helm chart (values-driven, BYO S3 support)
- Next.js pages: login, admin dashboard (event list), gallery password gate
- Shared types package

## What needs to be built next (in priority order)

### 1. Verify the foundation boots
```bash
cp .env.example .env   # fill in BETTER_AUTH_SECRET, ADMIN_EMAIL, ADMIN_PASSWORD
bun install
cd apps/api && bunx prisma migrate dev --name init
bun run dev
```
Fix any import/type errors that surface.

### 2. Gallery view page — `/gallery/[slug]/view`
The password gate at `/gallery/[slug]` already sets the cookie and redirects here.
This page needs to:
- Fetch `GET /api/gallery/:slug` (sends cookie automatically)
- Render a responsive photo grid using thumb URLs
- Lightbox on click — show display URL full screen
- "Upload your photos" button → opens upload modal
- Upload modal: name input (required) + file picker (multi-select) → calls
  `POST /api/gallery/:slug/upload` per file, shows per-file progress

### 3. Admin event creation — `/admin/events/new`
Form: name, slug (auto-generated from name, editable), description (optional), password.
On submit → `POST /api/events` → redirect to the new event's admin page.

### 4. Admin event detail — `/admin/events/[id]`
- Show event info + share link (`/gallery/[slug]`)
- Processing status bar (poll `GET /api/upload/events/:id/photos/status` every 2s while
  pending > 0)
- Bulk upload zone: drag-and-drop or file picker, uploads files concurrently (cap at 4
  parallel), shows per-file progress
- Photo grid (same component as gallery view, reuse it)
- Delete event button

### 5. Smoke-test Docker Compose end-to-end
```bash
docker compose up --build
```
Verify: admin login → create event → upload photos → processing completes →
share gallery link → guest unlocks → guest uploads → photos appear.

## Coding conventions
- TypeScript strict mode throughout — no `any`
- Effect for any async pipeline that can fail and should retry
- Zod for all external input validation (API request bodies)
- UI comes from Chakra UI v3. Tokens and semantic tokens live in `apps/web/src/components/theme.ts`;
  recipes only for primitives that are demonstrably duplicated. No inline `style={{}}`, no raw hex.
- Use semantic token *names* (`bg`, `surface`, `border`, `fg`, `fgMuted`, `fgSubtle`, `accent`,
  `danger`, `success`, `warning`) and named radii (`control`, `card`, `pill`) — never raw values.
  Dark mode is deliberately deferred; correct token names keep it to one `_dark` pass in `theme.ts`.
- Hover/focus are `_hover` / `_focusVisible`, never `e.currentTarget.style` mutations.
- `apps/web/src/app/globals.css` holds the four live `@keyframes` only; everything else is theme `globalCss`.
- Chakra's official skills are vendored at `.agents/skills/` (agent-agnostic). Re-run
  `cd apps/web && bun run typegen` whenever `theme.ts` changes (the `build` script does it for you).
- `PhotoGrid`'s measured container and cell boxes are off-limits to styling changes — the virtualizer
  positions rows from an estimate, not from measurement.
- Next.js: `"use client"` only where interactivity is needed; prefer server components for
  data fetching where it makes sense
- API responses: always `{ error: string }` on failure with appropriate HTTP status
- Commit subjects are load-bearing: release-please derives the version from them. `fix:` → patch,
  `feat:` → minor, `feat!:` → minor while below `1.0.0`, `chore:`/`docs:`/`test:` → no release.
  A real feature labelled `chore:` silently ships no bump, so label honestly.

## Running the Playwright e2e suite locally
```bash
docker compose up -d postgres minio minio-init          # MinIO comes from quay.io, not Docker Hub
cd apps/api && bunx prisma migrate deploy && bun prisma/seed.ts
bun run dev                                             # api :3001 + web :3000
bunx playwright test
```
The API needs a full env (see `.env.example`); `GALLERY_ENCRYPTION_KEY` is 64 lowercase hex chars,
and MinIO needs `S3_FORCE_PATH_STYLE=true`. The web dev script defaults `API_URL` to
`http://localhost:3001` — in Compose it is `http://api:3001`. BetterAuth rate-limits repeated
sign-ins, so leave ~2 minutes between full suite runs or the wrong-credentials test sees
"Too many requests" instead of the login error. Admin credentials are `ADMIN_EMAIL` /
`ADMIN_PASSWORD` from `.env` (`admin@example.com` / `changeme` by default).

## Known things to verify/fix
- `Bun.image()` API: check exact method signatures against
  https://bun.com/blog/bun-v1.3.14 — quality may be 0–1 not 0–100
- BetterAuth on Bun: smoke-test login early; if adapter issues appear check
  https://www.better-auth.com/docs/adapters/prisma
- The `requireGallerySession` middleware uses a manual cookie parser — consider
  switching to Hono's built-in `getCookie` from `hono/cookie`