apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pixshar.fullname" . }}-test-connection"
  labels:
    {{- include "pixshar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pixshar.web.fullname" . }}:{{ .Values.web.port }}']
  restartPolicy: Never
